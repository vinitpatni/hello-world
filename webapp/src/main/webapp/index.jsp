<h1>1. First forked the public repository. </h1>
<h2>2. Checking poll scm which will trigger the job to create the artifacts, if foundchanges in repo, publish them to target (Tomcat) in our case.
<h3>3. Retesting it
